# API Configuration
VITE_API_BASE_URL=http://localhost:8000/api
VITE_API_TIMEOUT=10000

# Cache Configuration (in milliseconds)
# Default: 5000 (5 seconds)
VITE_API_CACHE_TTL_MS=5000

# Environment
VITE_ENV=development

# App Version (for Sentry release tracking)
VITE_APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# Sentry Error Tracking & Performance Monitoring
# -----------------------------------------------------------------------------
# Get your DSN from https://sentry.io
# Leave empty to disable Sentry in development
#
# Setup steps:
#   1. Create account/project at sentry.io
#   2. Go to Settings -> Projects -> Your Project -> Client Keys (DSN)
#   3. Copy the DSN and paste below
#
VITE_SENTRY_DSN=

# Environment name shown in Sentry (default: from VITE_ENV)
# VITE_SENTRY_ENVIRONMENT=production

# Performance monitoring sample rate (0.0 to 1.0)
# 0.1 = 10% of page loads/API calls are traced
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
